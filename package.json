{
  "name": "MomDownloader",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "bag-mac":"electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds"
  },
  "dependencies": {
    "electron": "~1.7.8",
    "electron-packager": "^10.1.1",
    "cheerio": "^1.0.0-rc.2",
    "request": "^2.83.0"
  }
}
